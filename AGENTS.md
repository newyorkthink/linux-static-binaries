# AGENTS.md

本文件是本仓库中所有 AI coding agents（Codex、Claude Code、Copilot、Gemini CLI 等）的仓库级操作规范。

除本文件明确标记为“不可豁免”的规则外，用户在当前任务中的明确要求可以覆盖一般默认规则；标记为“不可豁免”的规则永久有效。

## 永久规则：禁止新增测试代码（不可豁免）

AI 永久禁止在本仓库新增、生成、补写、插入、恢复或迁移专门用于测试、冒烟测试或运行验证的可执行测试代码、测试脚本、测试 Workflow、测试 Job 或测试 Step。

允许的检查仅限于构建流程本身必须完成的静态完整性检查，例如：

- 检查下载或克隆是否成功。
- 核对本次动态解析出的上游 Tag 与对应 Commit 是否匹配。
- 检查预期构建产物是否存在且非空。
- 使用 `file`、`readelf`、`sha256sum` 等确认产物类型、静态链接属性和校验值。

不得加入“启动程序几秒后看退出码”“连接代理端口确认可用”等运行时冒烟测试，也不得为了验证构建临时创建测试 Workflow。

## 永久规则：禁止创建任何 Git 分支（不可豁免）

AI 永久禁止在本仓库创建任何 Git 分支。所有提交、推送和必要的历史重写只允许发生在远端已经存在的分支上，默认使用 `main`。

强制要求：

- 禁止 `git checkout -b`、`git switch -c`、`git branch <新名字>`、`git push origin HEAD:<新分支>`、`git worktree add -b` 以及任何等价的新建分支操作。
- 禁止通过 GitHub、`gh`、API 或网页为测试、PR、备份、并行任务、临时验证或回滚创建新分支。
- 禁止恢复或重建已经删除的分支名。
- 用户要求直接修改仓库时，完整检查后直接提交到现有 `main`；不得先建测试分支再合并。

## 永久规则：禁止锁定目标软件版本（不可豁免）

AI 永久禁止把本仓库“目标应用本身”的 Version、Tag 或 Commit SHA 作为长期固定值写入仓库。此规则适用于当前和今后加入的所有目标软件，不得只对某一个软件例外处理。

强制要求：

- 禁止在 `version.conf`、`build.conf`、构建脚本、Workflow、Action 或其他仓库文件中固定目标应用的某个 Version、Tag、Commit，并把它作为后续正式 Build 的源码选择依据。
- 每次 GitHub Actions 正式 Build 都必须从目标应用的官方上游动态解析“当前最新稳定版本 / Tag”。GitHub / GitLab 上游优先使用官方最新稳定 Release / Release API；没有 Release API 的上游使用官方稳定 Tag、stable channel 或等价官方机制。
- 默认必须排除 draft、prerelease、beta、nightly、insider、RC 等非稳定版本，除非该项目本身明确以对应渠道为正式目标且用户明确要求。
- 动态得到本次稳定 Tag 后，可以并且应当在当前 Build 内解析该 Tag 对应的不可变 Commit SHA，Checkout 后再次核对实际 Commit；该 Commit 仅用于本次构建的来源校验，不得永久写回仓库成为锁定值。
- `version.conf` 若继续存在，只能保存本仓库打包修订号或确有必要固定的构建环境 / 依赖项，不得保存目标应用本身的 Version、Tag、Commit。
- 依赖项、编译工具链、容器基础镜像、GitHub Actions Commit SHA 等如果为了兼容性、安全性或可复现性确实需要固定版本，必须单独判断；“构建依赖可锁定”不得被解释成“目标应用可锁定”。
- README、注释和 `VERSIONS.md` 不得把某个目标应用 Version / Tag / Commit 描述成当前或未来 Build 的固定输入。实际构建版本必须来自当次动态解析结果，并由 `dist/version.txt` 和 Release 的 `software_versions.json` 记录。

任何与“固定目标应用版本以保证可复现”相冲突的旧规则，一律以本条为准。可审计性通过“本次动态解析稳定 Tag → 解析 Commit → Checkout → 核对 Commit → 记录最终构建版本和 SHA-256”实现，而不是通过把目标应用锁死在仓库中实现。

## 永久规则：禁止使用 git revert 或新增反向提交（不可豁免）

AI 永久禁止在本仓库使用 `git revert`、GitHub 网页 Revert 操作，或创建专门抵消其他提交的反向 Commit。

如果用户要求删除、撤销或整个取消某个提交，必须先检查目标提交、父提交和后续提交关系，再通过最小范围的历史重写让目标提交不再属于 `main`；不得用新增反向 Commit 代替。

## 永久规则：本仓库必须独立运行（不可豁免）

本仓库中的代码、脚本、GitHub Actions、配置、构建、发布、检查、监督与自愈机制，只允许依赖本仓库自身内容、当前仓库的 GitHub 资源以及目标软件的官方上游资源；不得与其他代码仓库建立运行时依赖或耦合。

强制要求：

- 禁止为了构建、发布、检查、监督或自愈去读取、调用、触发、修改、写入、同步或依赖其他代码仓库的 Workflow、Artifact、Release、文件、状态或元数据。
- 禁止跨仓库 `repository_dispatch`、跨仓库 `workflow_call`、跨仓库 Artifact / Release 或其他等价运行时耦合。
- 目标软件的官方 GitHub / GitLab、官方网站、官方软件包仓库、官方 Release / API 属于正常供应链来源，不视为跨仓库耦合。
- 一次性迁移任务可以读取用户指定的源仓库作为静态参考；迁移完成后的正式代码和 Workflow 不得保留对源仓库的运行时依赖。
- 监督与自愈只能检查和处理本仓库自身的 Workflow、Job、Release、Release Assets、`software_versions.json` 和构建结果。

## 永久规则：每个 Build 成功后立即更新统一版本清单（不可豁免）

每个正式 Build 在自己的二进制成功上传到 `latest` Release 后，必须由当前 Build 自己立即更新共享 `software_versions.json` 中自己的唯一条目；禁止恢复中央 publisher、汇总 Job、轮询 Job 或其他独立等待队列统一收集多个 Build 结果后再写清单。

要求：

- 固定顺序为：构建静态二进制 → 上传当前软件固定资产名 → 核对 Release SHA-256 digest → 更新当前软件的 `software_versions.json` 唯一条目 → 校验写入结果。
- 每个 Build 只能修改与自己 `software_key` 对应的一个条目，必须保留清单中其他软件已有条目。
- `software_versions.json` 每个条目至少包含 `version`、`asset`、`sha256`。
- 多个 Build 并发时，只允许对版本清单的“读取 → 合并自己的唯一条目 → 上传 → 校验”临界区使用短时间互斥锁；不得因此串行全部编译过程。
- 锁必须能自动释放，并允许清理陈旧锁；构建失败、资产上传失败、Release digest 不一致或清单校验失败时不得写入错误条目。
- 不再为每个软件发布独立 `*-version.txt` 或 `*.sha256` Release 资产；版本与二进制 SHA-256 统一由 `software_versions.json` 管理。

## 永久规则：Release 默认只提供单文件（不可豁免）

本仓库面向用户的默认交付形态是“一个可直接下载并使用的文件”。新增或更新软件时，Release 默认只发布一个主程序单文件；不得因为上游 `make install` 同时安装 man page、locale、README、硬件数据库、示例、补全文档或其他非必需辅助文件，就自动改成 `.tar.xz`、`.tar.gz`、`.zip` 等压缩包。

强制要求：

- 单一可执行程序默认只发布该可执行文件，资产名直接使用程序名 / `software_key`。
- man page、locale、README、示例、开发文件、许可证副本、可选数据文件等，只要不影响主程序核心功能正常运行，就不作为 Release 附加资产，也不得为了携带它们而把主程序改成压缩包。
- 修改前仍必须核查上游标准安装目标、安装清单和运行时文件用途，区分“核心运行必需文件”与“文档 / 翻译 / 数据增强 / 可选组件”；核查结果写入对应软件 README。
- 如果上游附加数据只用于增强名称解析、翻译、帮助文本或其他非核心能力，保持单文件发布，并在 README 说明缺少这些外部数据时的影响。
- 如果软件脱离多个外部文件后确实无法正常完成其核心功能，不得擅自改成压缩包，也不得为了强行单文件而大幅魔改上游；必须先向用户说明原因，由用户决定是否接受例外方案。
- 只有用户明确要求压缩包、多文件安装树或其他封装方式时，才允许偏离单文件默认规则。
- 不得使用 AppImage、RunImage、Sharun、StaticX、自解压归档等包装层假装满足“单文件”，除非用户明确要求对应封装方式。

提交前必须使用 `file`、`readelf`、`sha256sum` 等做静态完整性检查；不得通过运行程序、连接端口等运行时冒烟测试代替该检查。

## 1. 仓库目标

本仓库用于从上游源码构建可分发的 Linux 静态二进制，重点是：

- 尽量生成真正静态链接的 ELF。
- 尽量减少宿主系统运行时依赖。
- 固定上游来源，并通过每次构建动态解析稳定版本及核对其 Commit 保持过程可审计、可追溯。
- 不向上游程序注入无关功能、遥测、广告、代理逻辑、持久化逻辑或额外网络请求。

对于适合直接静态编译的软件，禁止仅为了做成单文件而额外套 AppImage、RunImage、Sharun、StaticX 等包装层。只有在直接静态链接确实不可行且用户明确同意时，才考虑其他封装方式，并必须在对应 README 说明原因。

## 2. 修改前必须完整读取上下文

修改任何软件前，必须至少完整读取：

- 根目录 `README.md`。
- 本文件 `AGENTS.md`。
- 目标软件目录中的 `README.md`。
- 目标软件的 `version.conf`（若存在）和 `VERSIONS.md`。
- 目标软件构建脚本。
- 与本次改动直接相关的 Workflow / composite Action。

不得只看局部片段就直接修改。已经确认有效的构建命令、参数和 Release 资产名均视为稳定基线，除非当前任务明确要求，否则不得擅自改写；任何旧的“固定目标应用 Version / Tag / Commit”方式都不属于稳定基线，必须遵守本文件的永久动态版本规则。

## 3. 修改范围必须最小化

- 只修改完成当前任务所必需的文件。
- 不得顺手格式化、重命名、清理或重构无关软件。
- 不得为了风格统一而批量修改已经正常工作的构建脚本。
- 新增软件时创建独立目录，优先复用现有共享 Action 和目录结构。
- 如果现有公共逻辑可以复用，应扩展公共逻辑，而不是复制一套新的 Workflow。

## 4. 公共仓库隐私规则

本仓库是公开仓库。README、AGENTS、注释、版本记录、Workflow、构建日志说明和 Release 文本不得泄露用户个人运行环境。

禁止写入：

- 用户名、主机名、个人 HOME 路径。
- 私有 IP、实际公网 IP、设备名、SSID、服务器地址。
- Token、Cookie、密钥、账号、邮箱或任何认证信息。
- 仅从聊天、截图、日志中获知的个人发行版版本、内核版本、设备型号或网络环境。

编写可复用命令或说明时，端口、IP、用户名、路径等环境相关值必须使用 `<端口>`、`<IP地址>`、`<用户名>`、`<路径>` 等占位符；只有协议默认值、上游固定常量或构建系统客观固定值才可以直接写具体值。

## 5. 上游来源与动态稳定版本

每个正式 Build 必须从官方上游动态确定当前最新稳定版本 / Tag，并在本次 Build 内完成来源核查：

1. 从官方 Release API、稳定 Tag 列表、stable channel 或等价官方来源解析最新稳定 Version / Tag。
2. 排除非稳定版本。
3. 将本次动态 Tag 解析到对应 Commit SHA。
4. Checkout 该 Tag，并核对实际 Commit 与本次动态解析出的 Commit 一致。
5. 仅把最终动态版本写入构建生成的 `dist/version.txt` 和 Release 的 `software_versions.json`；不得把 Version / Tag / Commit 回写仓库作为下次 Build 的锁定输入。

更新构建逻辑时还必须核对许可证、标准安装集和构建方式是否发生变化。无法确认时不得猜测。

## 6. 版本历史不可覆盖

每个软件必须维护 `VERSIONS.md`，用于记录本仓库打包方式和资产结构的历史变化。

- 旧的打包变更记录不得为了“保持简洁”而删除或覆盖。
- `VERSIONS.md` 不得作为构建输入，也不得持久化某个目标应用 Version / Tag / Commit 作为当前锁定值。
- 上游版本变化由每次 Build 自动跟随最新稳定版，不需要提交一次“更新固定版本”的仓库改动。
- 构建方式、编译参数、标准运行时安装集处理或 Release 资产结构发生实质变化时，递增本仓库 `PACKAGE_REVISION` 并追加说明。
- 构建脚本必须生成 `dist/version.txt`，内容由“本次动态解析的上游版本 + 本仓库打包修订号”组成，例如 `<upstream-version>-r<revision>`；该文件仅供构建与统一版本清单使用，不提交仓库，也不作为独立 Release 资产发布。

## 7. 静态构建要求

对于标记为静态二进制的软件：

- 优先使用 musl 或上游明确支持的静态工具链。
- 不得把宿主机随机存在的 `.so` 依赖误认为静态构建成功。
- 正式产物必须在构建阶段用静态分析确认没有 ELF interpreter 和动态 `NEEDED` 项。
- 可以使用 `strip` 或链接器 `-s` 缩减体积，但禁止未经用户明确要求加入 UPX 或其他二次压缩壳。
- 不提交生成的二进制、对象文件、临时源码目录或 `dist/` 到 Git。

## 8. GitHub Actions 架构

正式构建入口固定为：

```text
.github/workflows/build.yml
```

共享构建 / 发布逻辑集中在：

```text
.github/actions/build-static/action.yml
```

要求：

- 默认只保留一个正式构建 Workflow；新增软件不得无理由新增一份独立 Workflow。
- Workflow 负责触发条件、Job 编排和软件选择；具体软件编译逻辑放在软件自己的 `build.sh`。
- 可以复用的安装、产物检查、Release 上传和统一版本清单更新逻辑放在共享 composite Action 中。
- 文档修改不得无意义触发二进制重建。
- Public 仓库可以正常使用 GitHub-hosted runner 完成必要构建，不得为了节省 Actions 分钟省略正确性所需步骤；但必须避免明显重复或错误触发。

Release 完整性监督固定使用：

```text
.github/workflows/supervise-release-integrity.yml
.github/scripts/supervise_release_integrity.py
```

- 监督 Workflow 只检查本仓库 `latest` Release 中 `software_versions.json` 与 Release Asset SHA-256 digest 的一致性，不写版本清单，也不充当中央 publisher。
- 发现单个可安全映射的异常时，只允许通过本仓库 `build.yml` 的 `workflow_dispatch` 重新构建对应软件一次；修复构建完成后再次监督，仍异常则失败并停止，禁止无限循环。
- 自动映射要求 `software_key`、软件目录名和 `workflow_dispatch.target` 保持一致；不能唯一、安全映射时必须停止，不得扩大为全量重建。
- 手动触发的构建首次失败时，监督可以只重新运行失败 Job 一次；第二次仍失败不得继续循环。Push 触发的构建失败不自动重跑。
- 监督 Workflow 属于发布完整性维护，不得加入程序启动、端口连接或其他运行时冒烟测试。

## 9. Release 规则

正式滚动 Release 固定使用：

```text
Tag: latest
Title: Latest
```

- 默认每个软件只发布一个可直接下载使用的固定名称单文件资产，版本更新时覆盖 `latest` 中自己的资产。
- man page、locale、README、示例、数据增强文件等非核心运行必需内容不得成为把单文件改成压缩包的理由。
- 单文件资产名默认直接使用软件可执行名 / `software_key`，例如 `microsocks`、`lshw`；禁止追加 `-x86_64-linux`、`-amd64-linux` 等架构 / 平台尾缀。若未来确实需要同一软件同时发布多架构，必须先经用户明确确认新的命名规则。
- 若核心功能确实必须依赖多个外部文件，必须先向用户说明并取得明确确认后再设计例外交付方式；不得自行发布压缩包。
- 所有软件的版本与 SHA-256 统一记录在 Release 根部唯一的 `software_versions.json`，不得为每个软件重复发布版本文本和 SHA-256 文本。
- `software_versions.json` 是 Release 元数据公共接口；更新时只能覆盖当前软件自己的条目，并保留其他软件条目。
- 上游许可证必须核查并完整保留；默认按上游原文件保存在对应软件目录中。除非许可证条款或当前任务明确要求，不额外制造独立 License Release 资产，也不为了携带许可证而把主程序改成压缩包。
- Release 资产名属于公共接口；已有使用者可能依赖它，未经用户明确要求不得随意改名。
- 构建失败、静态链接检查失败、核心运行必需文件核查失败、动态版本来源校验失败或 SHA-256 校验失败时，不得发布错误版本元数据。

## 10. 提交与分支

- 修改前读取 `main` 当前状态，避免覆盖并发改动。
- 用户要求直接修改仓库时，不创建测试分支；完整检查后直接提交到 `main`。
- 同一任务尽量保持一个逻辑 Commit；不得通过反复提交并等待 Actions 失败来试错。
- 提交前必须检查完整 diff、脚本语法、YAML 结构、路径、引号、权限、触发条件和 Release 资产名。
- 不得修改与当前任务无关的文件。

## 11. 文档要求

每个软件目录必须有 README，至少说明：

- 上游项目和许可证。
- 最新稳定版本的动态解析方式，以及本次 Build 如何核对 Tag 对应 Commit。
- 构建方式及为什么采用该方式。
- 上游标准安装集的核查结果、哪些文件属于核心运行必需、哪些只是文档 / 翻译 / 数据增强，以及单文件省略这些内容后的影响。
- 最终资产名。
- 基本运行方法和已知协议 / 功能限制。
- 与安全有关的监听、认证或权限注意事项。

文档只记录公共、可复用的技术事实，不记录用户个人环境。
